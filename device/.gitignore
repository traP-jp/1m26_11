# MicroPico / VS Code local generated state
device.code-workspace
.vscode/Pico-W-Stub/
.picowgo

# Host-side Python test cache
__pycache__/
*.py[cod]

# Ignore ordinary logs, but retain the historical PoC transcripts already tracked
*.log
!samples/*.log

# Production captures are local verification artifacts, not repository contents
samples/issue81-production/
